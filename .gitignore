_txt/
__pycache__/
*.pyc
.DS_Store

# Astro / Node build artifacts (generated, not committed)
node_modules/
dist/
.astro/
*.log
npm-debug.log*

# Firebase local state and credentials (web config is public; admin keys are not)
.firebase/
firebase-debug.log*
*-firebase-adminsdk-*.json
service-account*.json

# Local editor / tooling config (machine-specific, not committed)
.claude/
.vscode/

# Stray local exports / reports (not part of the project)
analytics-report.html
